@model IEnumerable<thcp.Models.Deduction>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Value)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DeductionType.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Measure.Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Value)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DeductionType.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Measure.Description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DeductionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DeductionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DeductionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
